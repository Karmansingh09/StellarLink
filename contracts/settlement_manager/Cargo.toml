[package]
name = "settlement_manager"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]
soroban-sdk = "21.1.0"

[dev-dependencies]
soroban-sdk = { version = "21.1.0", features = ["testutils"] }

[features]
testutils = ["soroban-sdk/testutils"]

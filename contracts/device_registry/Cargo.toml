[package]
name = "device_registry"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]
soroban-sdk = "21.1.0"

[dev-dependencies]
soroban-sdk = { version = "21.1.0", features = ["testutils"] }
ed25519-dalek = "2.1.1"

[features]
testutils = ["soroban-sdk/testutils"]
